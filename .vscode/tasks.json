{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "BMS: Start All",
      "type": "shell",
      "command": "npm run db:start && npm install --prefix server && npm install --prefix client && npm run dev",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "presentation": { "reveal": "always", "panel": "new" },
      "problemMatcher": []
    }
  ]
}
