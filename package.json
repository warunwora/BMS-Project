{
  "name": "bms-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
